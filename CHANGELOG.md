CHANGELOG
=========

0.1.0
-----

 * Add error pages for HTTP statuses 401, 402, 403, 404, 419, 429, 500, and 503
 * Add generic `4xx` and `5xx` fallback pages
 * Add application error view overrides and publishable package views
 * Add standalone error layout that uses the application's fonts and frontend assets
 * Add Laravel Head integration for error titles, viewport, and robots metadata
 * Add Laravel Boost skill for AI-assisted error page development
 * Add installation, customization, status page, and AI assistance documentation
